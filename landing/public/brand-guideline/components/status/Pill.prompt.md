The smaller, denser cousin of `Badge` — for status and plan cells inside admin tables.

```jsx
<Pill tone="active">Active</Pill>
<Pill tone="premium">Premium</Pill>
```

Use `Badge` in page chrome and card headers; `Pill` inside table rows.
