Pill-shaped state label. Tones are semantic, not decorative.

```jsx
<Badge tone="active" icon={<Icon name="shield" size={13} />}>Active</Badge>
<Badge tone="glass">Checking…</Badge>
<Badge tone="red">Admin</Badge>
```

The three `glass*` tones only work on the navy header; everywhere else use the solid tints.
