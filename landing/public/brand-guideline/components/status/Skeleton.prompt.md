Loading placeholder using the product's 1.4s grey shimmer.

```jsx
<Skeleton height={28} width={180} />
```

Match the real element's box. For whole-page loads ParkTag also runs a 3px red top progress bar (`ProgressBar`).
