One quiet centred sentence — ParkTag's empty states have no illustration and no call to action.

```jsx
<EmptyState>No recent contact requests yet.</EmptyState>
```

Write it as a fact ending in "yet", matching the product: "No print queue loaded yet."
