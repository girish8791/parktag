Phone entry with a fixed dial-code prefix. The whole shell takes the red border on focus, not just the input.

```jsx
<Field label="Mobile number"><PhoneInput placeholder="98765 43210" /></Field>
```

The owner's number is the thing ParkTag protects — always pair this field with a privacy note.
