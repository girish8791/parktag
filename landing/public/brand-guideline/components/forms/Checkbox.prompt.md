Native checkbox tinted with the brand red via `accent-color`.

```jsx
<Checkbox label="Physical sticker requested" />
```

For a consent checkbox on the activation flow, wrap it in the red-tint panel (`background: rgba(255,39,0,0.08)`, `border-radius: 18px`).
