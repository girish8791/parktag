Bare icon button for chrome: the header menu trigger, drawer close, modal close.

```jsx
<IconButton tone="onDark" label="Menu"><Icon name="menu" size={20} /></IconButton>
<IconButton tone="quiet" label="Close"><Icon name="close" size={20} /></IconButton>
```

Keep the tap area at least 44px in owner-facing mobile views.
