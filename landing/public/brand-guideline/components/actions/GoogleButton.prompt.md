Google sign-in button as it appears on both the owner and admin login cards, plus the "OR" divider that precedes it.

```jsx
<OrDivider />
<GoogleButton onClick={() => (location.href = "/api/auth/google")} />
```

Keep the official four-colour mark and the white/hairline treatment — this is the one button in the system that isn't ParkTag-branded.
