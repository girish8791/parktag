A single platform count. Lay them out in `repeat(auto-fill, minmax(160px, 1fr))` with a 14px gap.

```jsx
<StatCard label="Active tags" value="1,284" />
```

ParkTag's snapshot row shows plain totals only — resist adding trend arrows or percentages the backend doesn't produce.
