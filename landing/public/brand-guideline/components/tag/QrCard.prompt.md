Shows an owner's live QR at the product's fixed 180x180 size.

```jsx
<QrCard qrSrc={tag.qrUrl} hint="Scan to contact owner" />
```

The code is always on plain white — no gradient, no frame, nothing overlapping it, so a phone camera reads it first time.
