On-screen stand-in for the physical sticker, shown while an owner activates a tag they are holding.

```jsx
<StickerCard tagId="PT-0042-9KQ" />
```

For the real printed artwork use `assets/stickers/parktag-sticker.png` — don't rebuild the sticker in HTML.
