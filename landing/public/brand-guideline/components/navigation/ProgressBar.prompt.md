The 3px red top bar shown during navigation and form submits — ParkTag's global "something is happening" signal.

```jsx
<ProgressBar value={70} />
```

Pair with a button `loading` state rather than replacing it.
