Three-up progress strip for the activation flow. Translucent capsules on the gradient shell; the current step tints red.

```jsx
<StepStrip steps={["Scan tag", "Add vehicle", "Activate"]} active={1} />
```

Only used on the activation shell, where the background is the light-blue gradient — the translucency needs it.
